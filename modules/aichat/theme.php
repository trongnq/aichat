<?php
/**
 * AI Chat - theme.php
 * =====================================================================
 * AJAX chính được xử lý bởi modules/aichat/ajax.php (độc lập).
 * File này chỉ giữ để NukeViet không báo lỗi thiếu theme file.
 * Không làm gì cả — để NukeViet tiếp tục load funcs/main.php.
 * =====================================================================
 */
if (! defined('NV_MAINFILE')) {
    exit('Stop!!!');
}
// Không xử lý gì ở đây.
// Mọi POST AJAX đi qua modules/aichat/ajax.php trực tiếp.
